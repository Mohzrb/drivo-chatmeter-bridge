{
  "name": "chatmeter-zendesk-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Sync Chatmeter reviews into Zendesk (tickets + internal notes) with dedupe and cron.",
  "type": "module",
  "scripts": {
    "start": "echo \"Serverless on Vercel — no local start script\"",
    "lint": "echo \"noop\""
  },
  "engines": {
    "node": "18.x"
  }
}
